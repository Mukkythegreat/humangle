export const metadata = {
    title: "Confirm Email - HumAngle FOI",
    description: "Confirm your email address to finish your FOI submission.",
};

export default function ConfirmEmailLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
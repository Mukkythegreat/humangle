export const metadata = {
    title: "Send FOI Request - HumAngle FOI",
    description: "Send a Freedom of Information request to any public institution in Nigeria.",
};

export default function SendFOIRequestLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
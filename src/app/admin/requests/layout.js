export const metadata = {
    title: "Admin - HumAngle FOI",
    description: "Administer the HumAngle FOI (Freedom of Information Act) platform.",
};

export default function AdminRequestLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
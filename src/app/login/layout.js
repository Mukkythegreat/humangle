export const metadata = {
    title: "Login - HumAngle FOI",
    description: "Login to access the HumAngle FOI (Freedom of Information Act) platform.",
};

export default function LoginLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
export const metadata = {
    title: "Reset Password - HumAngle FOI",
    description: "Reset your password to access the HumAngle FOI (Freedom of Information Act) platform.",
};

export default function ResetPasswordLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
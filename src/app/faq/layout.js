export const metadata = {
    title: "FAQ - HumAngle FOI",
    description: "Get answers to frequently asked questions about the HumAngle FOI (Freedom of Information Act).",
};

export default function FAQLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
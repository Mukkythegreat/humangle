export const metadata = {
    title: "FOI Request - HumAngle FOI",
    description: "View all Freedom of Information Act (FOI) requests made.",
};

export default function FOIRequestLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
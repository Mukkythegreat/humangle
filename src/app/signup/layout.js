export const metadata = {
    title: "Sign Up - HumAngle FOI",
    description: "Sign up to access the HumAngle FOI (Freedom of Information Act) platform.",
};

export default function SignUpLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
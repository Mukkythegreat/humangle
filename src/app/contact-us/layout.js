export const metadata = {
    title: "Contact Us - HumAngle FOI",
    description: "Fill the form to contact us for inquiries or any questions you may have.",
};

export default function CantactUsLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}